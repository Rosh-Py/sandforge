/** Application-wide brand constants. Change these to rebrand the entire app. */
export const APP_NAME = 'SandForge';
export const APP_NAME_UPPER = 'SANDFORGE';

/** The `source` identifier used in postMessage between the sandbox iframe and the parent. */
export const SANDBOX_SOURCE = 'sandforge-sandbox';

/** The Monaco editor custom theme name. */
export const EDITOR_THEME = 'sandforge-dark';
